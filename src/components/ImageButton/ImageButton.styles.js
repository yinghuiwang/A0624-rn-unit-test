import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  rootView: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    width: 100,
    height: 50,
    borderRadius: 4,
    borderWidth: 1,
    borderColor: '#E2E2E2',
  }
});